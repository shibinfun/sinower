<nav class="bg-slate-900 border-b border-slate-800 fixed w-full z-20 top-0 start-0">
  <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
    <a href="<%= root_path %>" class="flex items-center space-x-3 rtl:space-x-reverse">
      <span class="self-center text-2xl font-semibold whitespace-nowrap text-blue-400 tracking-wider">SINOWER</span>
    </a>
    <div class="flex md:order-2 space-x-3 md:space-x-0 rtl:space-x-reverse">
      <% if user_signed_in? %>
        <div class="flex items-center space-x-4">
          <span class="text-slate-300 text-sm"><%= current_user.email %></span>
          <%= button_to "登出", destroy_user_session_path, method: :delete, class: "text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 text-center" %>
        </div>
      <% else %>
        <div class="flex space-x-2">
          <%= link_to "登录", new_user_session_path, class: "text-slate-300 hover:text-white px-3 py-2 text-sm font-medium" %>
          <%= link_to "注册", new_user_registration_path, class: "text-white bg-blue-600 hover:bg-blue-700 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 text-center" %>
        </div>
      <% end %>
    </div>
    <div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1" id="navbar-sticky">
      <ul class="flex flex-col p-4 md:p-0 mt-4 font-medium border border-slate-800 rounded-lg bg-slate-900 md:space-x-8 rtl:space-x-reverse md:flex-row md:mt-0 md:border-0 md:bg-slate-900">
        <li>
          <a href="<%= root_path %>" class="block py-2 px-3 text-white bg-blue-700 rounded-sm md:bg-transparent md:text-blue-500 md:p-0" aria-current="page">首页</a>
        </li>
        <li>
          <a href="#" class="block py-2 px-3 text-slate-300 rounded-sm hover:bg-slate-800 md:hover:bg-transparent md:hover:text-blue-500 md:p-0">产品中心</a>
        </li>
        <li>
          <a href="#" class="block py-2 px-3 text-slate-300 rounded-sm hover:bg-slate-800 md:hover:bg-transparent md:hover:text-blue-500 md:p-0">关于我们</a>
        </li>
        <% if user_signed_in? && current_user.admin? %>
          <li>
            <a href="<%= admin_root_path %>" class="block py-2 px-3 text-yellow-400 font-bold rounded-sm hover:bg-slate-800 md:hover:bg-transparent md:hover:text-yellow-500 md:p-0">后台管理</a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
